# Preserve diagnostic command output verbatim; aligned trailing spaces and CRs
# are part of the captured evidence rather than source formatting errors.
work/** whitespace=-trailing-space,-blank-at-eof,cr-at-eol
